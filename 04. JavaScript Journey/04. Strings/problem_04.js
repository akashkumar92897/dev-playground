//Try to change 4th character of a given string. Were you able to do it?

let money = "Five Thousand Dollar";

//No.   --->   Strings in JavaScript are immutable, so you cannot change a character directly; `replace()` creates and returns a new string instead of modifying the original one.
