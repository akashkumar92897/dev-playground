//Use typeof operator to find the datatype of the string in the ast question.
let str = "Akash"
let n = 20;

let ans = str + n;

console.log(typeof(ans));
//Because adding a number to a string in JavaScript converts the number to a string, the result is a string and typeof returns "string".