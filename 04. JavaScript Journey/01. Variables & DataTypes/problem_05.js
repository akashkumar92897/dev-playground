//Try to add a new key to the const object in problem 3 were you able to do it?
const obj = {
    name: "Akash"
}

console.log(obj);

obj.gender = "M";
obj.age = 21;
console.log(obj);

//You can add new keys to a const object because const only prevents reassignment of the object reference, not modification of its properties.
