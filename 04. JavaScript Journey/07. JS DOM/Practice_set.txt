1. Create a navbar and change the color of its first element to red.
2. Create a table without tbody. Now use "View page source" button to check whether it has a tbody or not.
3. Create an element with 3 children. Now change the color of first and last element to green.                   =>          ✅Done in searchingTheDOM.html file
4. Write a JS code to change background of all <li> tags to cyan.
5. Which of the following is used to look for the farthese ancestor that matches a given CSS selector.           =>          ✅Farthest ancestor is none of these, b/c only nearest ancestor is available.
    (a) matches   (b) closet   (c) contains   (d) None of these