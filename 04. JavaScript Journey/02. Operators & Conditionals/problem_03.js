//Write a JS program to find whether a number is divisible by 2 & 3.
let num = 18;
if (num%2 == 0 && num%3 == 0){
    console.log("Number " + num + " is divisible by 2 & 3 both.")
} else {
    console.log("Number " + num + " is not divisible by 2 & 3 both.")
}