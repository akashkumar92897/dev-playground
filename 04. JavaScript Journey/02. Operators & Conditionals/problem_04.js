//Write a JS program to find whether a number is divisible by either 2 or 3.
let num = 11;
if (num%2 == 0 || num%3 == 0){
    console.log("Number " + num + " is divisible by either 2 or 3.")
} else {
    console.log("Number " + num + " is neither divisible by 2 nor by 3.")
}